(function() {

  const GoogleMapView = {}

  // zoom level for Google Map
  const DEFAULT_ZOOM = 14
  const STATUS_OK = 200

  /* Renders a map for the given entry into the provided mapElement element. */
  GoogleMapView.render = (mapElement, entryData) => {
    // TODO
  }

  window.GoogleMapView = GoogleMapView

})()
